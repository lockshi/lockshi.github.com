/*
Stores configuration variables expected to be changed by at dev time only
*/

(function(){
   window.OpenChat = window.OpenChat || {};
   window.OpenChat.Config = {
       chat : {
           showIframeInViewport : false
       }
   };
})();
